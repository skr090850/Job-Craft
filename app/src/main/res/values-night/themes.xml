<!-- res/values-night/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark Mode -->
    <style name="Theme.JobCraft" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="textPrimaryColor">@color/white</item>
        <item name="textSecondaryColor">@color/lightGray</item>

<!--        <item name="primaryBgColor">@color/blueViolet</item>-->
        <item name="primaryBgColor">@color/black</item>
        <item name="secondaryBgColor">@color/white</item>

        <item name="appColor">@color/blue</item>
        <item name="appBgColor">@color/white</item>

        <item name="btnBgColor">@color/white</item>
        <item name="btnTextColor">@color/blue</item>

        <item name="primaryColor">@color/white</item>
        <item name="secondaryColor">@color/violet</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>